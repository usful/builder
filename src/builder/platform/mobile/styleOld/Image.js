export default {
  backfaceVisibility: String, // enum('visible', 'hidden')
  backgroundColor: String, // color
  borderColor: String, //color
  borderRadius: Number,
  borderWidth: Number,
  opacity: Number,
  overflow: String, //enum('visible', 'hidden')
  resizeMode: String, //enum('cover', 'contain', 'stretch')


  overlayColor: String, // android
  tintColor: String // ios color
}